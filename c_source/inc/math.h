#ifndef MATH_H
#define MATH_H

#include <stdint.h>

/**
 * @brief Add two 16-bit numbers
 *
 * @param a_param First number
 * @param b_param Second number
 * @return uint32_t Sum of a and b
 */
uint32_t add(uint16_t a_param, uint16_t b_param);

#endif  // MATH_H